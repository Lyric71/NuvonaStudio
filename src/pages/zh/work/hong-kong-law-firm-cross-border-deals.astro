---
import Layout from '../../../layouts/Layout.astro';
import NavBar from '../../../components/NavBar.astro';
import ContactCTA from '../../../components/ContactCTA.astro';
---

<Layout
  lang="zh"
  title="香港律所合伙人怎么靠LinkedIn拿到跨境案源 | Nuvora Studio"
  description="一家香港律所让资深合伙人成了LinkedIn上跨境案源的入口。两个新委托直接归因于平台曝光。"
  abstract="高管IP打造、LinkedIn内容服务和全员传播，帮一家香港跨境律所在LinkedIn上获得了案源。"
>
  <NavBar lang="zh" />
  <main id="main-content">

    <!-- Hero -->
    <section class="cs-hero" aria-labelledby="cs-heading">
      <div class="container">
        <div class="cs-hero__inner">
          <div class="cs-hero__meta">
            <span class="cs-hero__number">实战案例</span>
            <div class="cs-hero__tags">
              <span class="cs-tag-pill">高管IP打造</span>
              <span class="cs-tag-pill">LinkedInLinkedIn内容服务</span>
              <span class="cs-tag-pill">全员传播</span>
            </div>
          </div>
          <h1 id="cs-heading" class="cs-hero__h1">
            香港律所的几位老合伙人，在LinkedIn上变成了跨境案源的入口。<br /><em>能力一直在，缺的是被看见。</em>
          </h1>
          <p class="cs-hero__deck">
            专业能力具备，但缺乏市场曝光。通过高管影响力建设，带来实际业务机会。
          </p>
        </div>
      </div>
    </section>

    <!-- Stats bar -->
    <section class="cs-stats-bar" aria-label="核心指标">
      <div class="container">
        <div class="cs-stats-bar__inner">
          <div class="cs-stat">
            <span class="cs-stat__value">2</span>
            <span class="cs-stat__label">个通过 LinkedIn 获得的委托项目</span>
          </div>
          <div class="cs-stat">
            <span class="cs-stat__value">3,800</span>
            <span class="cs-stat__label">累计粉丝</span>
          </div>
          <div class="cs-stat">
            <span class="cs-stat__value">12,000</span>
            <span class="cs-stat__label">单条内容最高曝光</span>
          </div>
        </div>
      </div>
    </section>

    <!-- Article body -->
    <article class="cs-body" aria-label="案例正文">
      <div class="container">
        <div class="cs-content">

          <section>
            <h2>客户概况</h2>
            <p>
              香港的一家律所，做跨境并购、企业重组和涉华商业争议的。靠口碑和私人引荐吃了几十年的饭，一直运转得不错，但增长到了一个瓶颈。<strong>他们想让PE基金、企业发展部门、还有在大中华区活跃的跨国公司能直接在网上找到自己。</strong>
            </p>
            <p>
              几位资深合伙人做过的交易非常多，但你去看他们的LinkedIn主页完全看不出来。有一个合伙人的资料页上居然还挂着他四年前就走了的上一家所。还有一位，2019年之后就没登录过。
            </p>
          </section>

          <section>
            <h2>执行路径</h2>
            <p>
              三位合伙人，三个方向。我们特意不让三个律师发差不多的东西。
            </p>
            <p>
              <strong>一位走大中华区并购趋势观察</strong>，写交易模式、买方行为、区域动向。第二位盯着监管风险，分析内地政策变了之后对跨境交易有什么影响。第三位专门写跨国合资企业的争议解决。每个人的个人主页都重新做了，内容计划也是按各自的定位定制的。
            </p>
            <p>
              整个项目卡得最厉害的地方，是让第一批帖子发出去。律师嘛，什么都要过一遍。合规部逐字逐句审，第一轮走了十一天。做LinkedIn内容服务的人都知道，这个速度基本等于项目没法往下做了。我们后来搭了一套精简审核流程把周转压到48小时，合规那边花了几个礼拜才适应，之后项目才总算跑顺了。
            </p>
            <p>
              企业页面每周更新一次，内容跟实时事件挂钩。北京出了新政策的时候跟进分析，媒体报道了跨境交易就发专业点评。重点是要让人看到这家所在关注当下正在发生的事情，不是在翻旧文章。
            </p>
            <p>
              <strong>全员传播这块拉了15个律师和法律顾问参加。</strong>每周发一套内容包给他们，里面有预写好的法律动态帖、律所观点稿、交易进展之类的。每个月做一次复盘，主要是怕参与率往下掉。
            </p>
          </section>

          <section>
            <h2>拿到了什么</h2>
            <p>
              前两个季度，<strong>2个跨境新委托可以直接归因到LinkedIn。</strong>听起来不多。但这种律所一笔委托的账单动辄七位数，2个已经相当有分量了。
            </p>
            <p>
              三位合伙人四个月合计粉丝到了3,800。有一篇写内地某项监管政策变动的帖子拿到12,000次阅读，被两个财经号转了。企业页面从600涨到1,700粉。全员传播参与率大概在55%左右，第三个月有点回落，那段时间几个律师在忙一个大项目，后来恢复了。
            </p>
            <blockquote>
              "以前得靠别人牵线才有机会。现在客户自己找上门，开口之前就已经清楚我们能做什么了。"
            </blockquote>
          </section>

        </div>
      </div>
    </article>

    <!-- Back link -->
    <section class="cs-back" aria-label="导航">
      <div class="container">
        <a href="/zh/work" class="cs-back__link">← 返回全部案例</a>
      </div>
    </section>

    <ContactCTA lang="zh" />
  </main>
</Layout>

<style>
  /* ── Hero ── */
  .cs-hero {
    background: var(--deep-navy);
    padding-block: clamp(3rem, 6vw, 5rem) clamp(3rem, 5vw, 4.5rem);
    position: relative;
    overflow: hidden;
  }
  .cs-hero::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse 60% 80% at 80% 30%, rgba(6,182,212,0.10) 0%, transparent 60%);
    pointer-events: none;
  }

  .cs-hero__inner {
    position: relative;
    z-index: 1;
    max-width: 780px;
  }

  .cs-hero__meta {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--sp-4);
    margin-bottom: var(--sp-6);
  }

  .cs-hero__number {
    font-family: var(--font-body);
    font-size: var(--fs-xs);
    font-weight: 700;
    letter-spacing: 0.10em;
    text-transform: uppercase;
    color: var(--cyan);
  }

  .cs-hero__tags {
    display: flex;
    flex-wrap: wrap;
    gap: var(--sp-2);
  }

  .cs-tag-pill {
    display: inline-block;
    padding: 2px 10px;
    border-radius: 999px;
    background: rgba(255,255,255,0.10);
    border: 1px solid rgba(255,255,255,0.16);
    color: rgba(255,255,255,0.75);
    font-family: var(--font-body);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .cs-hero__h1 {
    font-family: var(--font-display);
    font-size: clamp(2rem, 4.5vw, 3.25rem);
    font-weight: 700;
    line-height: 1.12;
    letter-spacing: -0.01em;
    color: var(--white);
    margin-bottom: var(--sp-6);
  }
  .cs-hero__h1 em {
    color: rgba(255,255,255,0.45);
    font-weight: 300;
    font-style: normal;
  }

  .cs-hero__deck {
    font-family: var(--font-body);
    font-size: var(--fs-md);
    color: rgba(255,255,255,0.68);
    line-height: 1.75;
    max-width: 600px;
  }

  /* ── Stats bar ── */
  .cs-stats-bar {
    background: var(--off-white);
    border-bottom: 1px solid var(--border);
    padding-block: var(--sp-8);
  }

  .cs-stats-bar__inner {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--sp-6);
    max-width: 680px;
    margin-inline: auto;
  }

  .cs-stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 4px;
  }

  .cs-stat__value {
    font-family: var(--font-display);
    font-size: clamp(1.75rem, 3vw, 2.25rem);
    font-weight: 700;
    color: var(--deep-navy);
    letter-spacing: -0.02em;
  }

  .cs-stat__label {
    font-family: var(--font-body);
    font-size: 0.72rem;
    color: var(--text-3);
    font-weight: 500;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  /* ── Body ── */
  .cs-body {
    padding-block: clamp(3rem, 6vw, 5rem);
    background: var(--white);
  }

  .cs-content {
    max-width: 680px;
    margin-inline: auto;
  }

  .cs-content section {
    margin-bottom: clamp(2.5rem, 4vw, 3.5rem);
  }

  .cs-content h2 {
    font-family: var(--font-display);
    font-size: clamp(1.25rem, 2vw, 1.6rem);
    font-weight: 700;
    color: var(--deep-navy);
    letter-spacing: -0.01em;
    line-height: 1.3;
    margin-bottom: var(--sp-5);
  }

  .cs-content p {
    font-family: var(--font-body);
    font-size: var(--fs-md);
    color: var(--text);
    line-height: 1.85;
    margin-bottom: var(--sp-5);
  }

  .cs-content blockquote {
    border-left: 3px solid var(--sky);
    margin: var(--sp-8) 0;
    padding: var(--sp-1) var(--sp-6);
    font-family: var(--font-body);
    font-size: var(--fs-md);
    font-style: italic;
    color: var(--text-2);
    line-height: 1.75;
  }

  /* ── Back link ── */
  .cs-back {
    padding-block: var(--sp-8);
    background: var(--off-white);
    border-top: 1px solid var(--border);
  }

  .cs-back__link {
    font-family: var(--font-body);
    font-size: var(--fs-sm);
    font-weight: 500;
    color: var(--text-2);
    transition: color var(--t-fast), transform var(--t-fast);
  }
  .cs-back__link:hover { color: var(--sky); transform: translateY(-3px); }

  /* ── Responsive ── */
  @media (max-width: 640px) {
    .cs-hero__meta { flex-direction: column; align-items: flex-start; }
    .cs-stats-bar__inner { grid-template-columns: 1fr; gap: var(--sp-5); }
  }
</style>
