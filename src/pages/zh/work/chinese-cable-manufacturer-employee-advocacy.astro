---
import Layout from '../../../layouts/Layout.astro';
import NavBar from '../../../components/NavBar.astro';
import ContactCTA from '../../../components/ContactCTA.astro';
---

<Layout
  lang="zh"
  title="中国线缆厂40个工程师上LinkedIn的故事 | Nuvora Studio"
  description="一家中国线缆制造商把40个驻外工程师拉上了LinkedIn。60天，16万次曝光。"
  abstract="全员传播和LinkedIn培训怎么帮一家中国线缆厂在海外打开了声量。"
>
  <NavBar lang="zh" />
  <main id="main-content">

    <!-- Hero -->
    <section class="cs-hero" aria-labelledby="cs-heading">
      <div class="container">
        <div class="cs-hero__inner">
          <div class="cs-hero__meta">
            <span class="cs-hero__number">案例</span>
            <div class="cs-hero__tags">
              <span class="cs-tag-pill">LinkedInLinkedIn内容服务</span>
              <span class="cs-tag-pill">全员传播</span>
              <span class="cs-tag-pill">培训</span>
            </div>
          </div>
          <h1 id="cs-heading" class="cs-hero__h1">
            中国线缆厂的40个工程师，从微信群走上了LinkedIn。<br /><em>60天，16万次曝光。</em>
          </h1>
          <p class="cs-hero__deck">
            60 天内，建立员工影响力体系。
          </p>
        </div>
      </div>
    </section>

    <!-- Stats bar -->
    <section class="cs-stats-bar" aria-label="核心指标">
      <div class="container">
        <div class="cs-stats-bar__inner">
          <div class="cs-stat">
            <span class="cs-stat__value">40</span>
            <span class="cs-stat__label">位员工参与</span>
          </div>
          <div class="cs-stat">
            <span class="cs-stat__value">16万</span>
            <span class="cs-stat__label">60 天总曝光</span>
          </div>
          <div class="cs-stat">
            <span class="cs-stat__value">1,400</span>
            <span class="cs-stat__label">公司主页新增粉丝</span>
          </div>
        </div>
      </div>
    </section>

    <!-- Article body -->
    <article class="cs-body" aria-label="案例正文">
      <div class="container">
        <div class="cs-content">

          <section>
            <h2>情况</h2>
            <p>
              做电力电缆和光纤的，产品卖到45个国家。销售日常基本全在微信上。<strong>但非洲、东南亚、南美的经销商和工程客户跑到LinkedIn上一搜，什么都搜不到。</strong>企业页面上挂着一个糊了的logo，简介就两行字，上一条帖子是八个月以前发的。
            </p>
            <p>
              更大的问题是，公司有大批技术人员长期驻在海外。装电缆的、跑项目会的、每个礼拜去客户现场的。他们手里有大量一线故事，随便讲讲都能打动买家，只是从来没有把这些东西放到LinkedIn上去。
            </p>
          </section>

          <section>
            <h2>我们的做法</h2>
            <p>
              没有先动企业页面。先进了培训教室。
            </p>
            <p>
              两场工作坊，隔两天做一场。第一场讲基本功：好的LinkedIn个人页面长什么样、算法凭什么推你的内容、为什么在别人帖子下面评论跟自己发帖一样重要。第二场直接上手干，每个人在现场就写出来了自己第一篇帖子。有几个写得挺糙的，但有个哥们写了三句话讲一个变压器安装现场，写得其实相当好，我们基本没怎么动。
            </p>
            <p>
              <strong>五个国家、40个人报名参加了全员传播计划。</strong>每周发一套内容包：两三篇可以自己改改就发的模板帖、一篇相关行业报道、再加一句提示，类似"拍张你上次去工地的照片，配两句话说说你看到了什么"。
            </p>
            <p>
              参与度用一个排行榜来追踪。没搞什么花活，也没有奖品，就是把名字列出来而已。但这个东西的激励效果比你想象的大。
            </p>
            <p>
              企业页面走双语路线，中英都发。项目施工实拍、产品认证进展、工程节点。页面的功能说白了就是个"着陆页"——别人看了工程师的帖子，去查一下公司，得能找到东西。
            </p>
          </section>

          <section>
            <h2>数据和后续</h2>
            <p>
              第一个月参与率58%，算是不错。我们原来预期应该更高一点的。到第二个月就好了，大家看到同事发的帖子有人点赞评论，自己也愿意跟上了。
            </p>
            <p>
              <strong>40个人的合计触达：60天16万次曝光。</strong>企业页面从200粉涨到1,400。
            </p>
            <p>
              有件事我们经常提起。在尼日利亚的一个现场工程师发了张变电站电缆施工的照片，帖子很短，也没什么修饰。当地一个经销商刷到了，去看了一下公司页面，当天就发了询盘过来。一篇帖子，一张照片，一个询盘。全员传播跑通了是什么样子？就是这样。
            </p>
            <p>
              合作结束后，客户市场部自己接手了这个项目。后来又拉了第二批人进来做。
            </p>
          </section>

        </div>
      </div>
    </article>

    <!-- Back link -->
    <section class="cs-back" aria-label="导航">
      <div class="container">
        <a href="/zh/work" class="cs-back__link">← 返回全部案例</a>
      </div>
    </section>

    <ContactCTA lang="zh" />
  </main>
</Layout>

<style>
  /* ── Hero ── */
  .cs-hero {
    background: var(--deep-navy);
    padding-block: clamp(3rem, 6vw, 5rem) clamp(3rem, 5vw, 4.5rem);
    position: relative;
    overflow: hidden;
  }
  .cs-hero::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse 60% 80% at 80% 30%, rgba(6,182,212,0.10) 0%, transparent 60%);
    pointer-events: none;
  }

  .cs-hero__inner {
    position: relative;
    z-index: 1;
    max-width: 780px;
  }

  .cs-hero__meta {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--sp-4);
    margin-bottom: var(--sp-6);
  }

  .cs-hero__number {
    font-family: var(--font-body);
    font-size: var(--fs-xs);
    font-weight: 700;
    letter-spacing: 0.10em;
    text-transform: uppercase;
    color: var(--cyan);
  }

  .cs-hero__tags {
    display: flex;
    flex-wrap: wrap;
    gap: var(--sp-2);
  }

  .cs-tag-pill {
    display: inline-block;
    padding: 2px 10px;
    border-radius: 999px;
    background: rgba(255,255,255,0.10);
    border: 1px solid rgba(255,255,255,0.16);
    color: rgba(255,255,255,0.75);
    font-family: var(--font-body);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .cs-hero__h1 {
    font-family: var(--font-display);
    font-size: clamp(2rem, 4.5vw, 3.25rem);
    font-weight: 700;
    line-height: 1.12;
    letter-spacing: -0.01em;
    color: var(--white);
    margin-bottom: var(--sp-6);
  }
  .cs-hero__h1 em {
    color: rgba(255,255,255,0.45);
    font-weight: 300;
    font-style: normal;
  }

  .cs-hero__deck {
    font-family: var(--font-body);
    font-size: var(--fs-md);
    color: rgba(255,255,255,0.68);
    line-height: 1.75;
    max-width: 600px;
  }

  /* ── Stats bar ── */
  .cs-stats-bar {
    background: var(--off-white);
    border-bottom: 1px solid var(--border);
    padding-block: var(--sp-8);
  }

  .cs-stats-bar__inner {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--sp-6);
    max-width: 680px;
    margin-inline: auto;
  }

  .cs-stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 4px;
  }

  .cs-stat__value {
    font-family: var(--font-display);
    font-size: clamp(1.75rem, 3vw, 2.25rem);
    font-weight: 700;
    color: var(--deep-navy);
    letter-spacing: -0.02em;
  }

  .cs-stat__label {
    font-family: var(--font-body);
    font-size: 0.72rem;
    color: var(--text-3);
    font-weight: 500;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  /* ── Body ── */
  .cs-body {
    padding-block: clamp(3rem, 6vw, 5rem);
    background: var(--white);
  }

  .cs-content {
    max-width: 680px;
    margin-inline: auto;
  }

  .cs-content section {
    margin-bottom: clamp(2.5rem, 4vw, 3.5rem);
  }

  .cs-content h2 {
    font-family: var(--font-display);
    font-size: clamp(1.25rem, 2vw, 1.6rem);
    font-weight: 700;
    color: var(--deep-navy);
    letter-spacing: -0.01em;
    line-height: 1.3;
    margin-bottom: var(--sp-5);
  }

  .cs-content p {
    font-family: var(--font-body);
    font-size: var(--fs-md);
    color: var(--text);
    line-height: 1.85;
    margin-bottom: var(--sp-5);
  }

  .cs-content blockquote {
    border-left: 3px solid var(--sky);
    margin: var(--sp-8) 0;
    padding: var(--sp-1) var(--sp-6);
    font-family: var(--font-body);
    font-size: var(--fs-md);
    font-style: italic;
    color: var(--text-2);
    line-height: 1.75;
  }

  /* ── Back link ── */
  .cs-back {
    padding-block: var(--sp-8);
    background: var(--off-white);
    border-top: 1px solid var(--border);
  }

  .cs-back__link {
    font-family: var(--font-body);
    font-size: var(--fs-sm);
    font-weight: 500;
    color: var(--text-2);
    transition: color var(--t-fast), transform var(--t-fast);
  }
  .cs-back__link:hover { color: var(--sky); transform: translateY(-3px); }

  /* ── Responsive ── */
  @media (max-width: 640px) {
    .cs-hero__meta { flex-direction: column; align-items: flex-start; }
    .cs-stats-bar__inner { grid-template-columns: 1fr; gap: var(--sp-5); }
  }
</style>
