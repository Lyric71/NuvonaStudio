---
import Layout from '../../../layouts/Layout.astro';
import NavBar from '../../../components/NavBar.astro';
import ContactCTA from '../../../components/ContactCTA.astro';
---

<Layout
  lang="zh"
  title="瑞典聚合物厂商如何用LinkedIn拿下航空航天客户 | Nuvora Studio"
  description="一家瑞典高分子材料厂商成了航空采购商在LinkedIn上最先搜到的品牌。从340个粉丝到世界500强主动上门。"
  abstract="一家瑞典航空材料品牌从LinkedIn上340粉丝做到世界500强主动找过来的过程。"
>
  <NavBar lang="zh" />
  <main id="main-content">

    <!-- Hero -->
    <section class="cs-hero" aria-labelledby="cs-heading">
      <div class="container">
        <div class="cs-hero__inner">
          <div class="cs-hero__meta">
            <span class="cs-hero__number">实战案例</span>
            <div class="cs-hero__tags">
              <span class="cs-tag-pill">咨询</span>
              <span class="cs-tag-pill">LinkedInLinkedIn内容服务</span>
              <span class="cs-tag-pill">LinkedInLinkedIn广告服务</span>
            </div>
          </div>
          <h1 id="cs-heading" class="cs-hero__h1">
            瑞典聚合物厂商，怎么让航空采购第一个想到自己？<br /><em>主页340个粉丝的时候，没人信。</em>
          </h1>
          <p class="cs-hero__deck">
            重塑品牌呈现后，成为目标客户主动搜索的对象。
          </p>
        </div>
      </div>
    </section>

    <!-- Stats bar -->
    <section class="cs-stats-bar" aria-label="核心指标">
      <div class="container">
        <div class="cs-stats-bar__inner">
          <div class="cs-stat">
            <span class="cs-stat__value">1,900</span>
            <span class="cs-stat__label">新增粉丝</span>
          </div>
          <div class="cs-stat">
            <span class="cs-stat__value">3.2%</span>
            <span class="cs-stat__label">互动率</span>
          </div>
          <div class="cs-stat">
            <span class="cs-stat__value">61</span>
            <span class="cs-stat__label">条销售线索</span>
          </div>
        </div>
      </div>
    </section>

    <!-- Article body -->
    <article class="cs-body" aria-label="案例正文">
      <div class="container">
        <div class="cs-content">

          <section>
            <h2>背景</h2>
            <p>
              瑞典的一家高性能聚合物复合材料制造商，给飞机做零部件材料。他们的材料此刻就在天上飞着。但LinkedIn主页完全看不出这些——340个粉丝，没有发帖节奏，品牌视觉好几年没换过。另一边，一个产品线比它还窄的竞争对手，粉丝是它三倍，每个季度都在投LinkedIn广告。
            </p>
            <blockquote>
              "有些单子我们本来该拿下的，结果丢了。因为采购方去LinkedIn一看，觉得我们像个小作坊。"
            </blockquote>
          </section>

          <section>
            <h2>切入方式</h2>
            <p>
              先做了一个咨询项目。完整的LinkedIn诊断、竞品拆解、90天行动计划。诊断出来的结论，其实跟销售总监自己说的一样：<strong>页面上发的全是自说自话。</strong>公司获了什么奖、办公室翻新了、周年庆。没有一条是买家会点进去看的。面向采购工程师的内容？没有。面向研发总监的材料评测？也没有。
            </p>
            <p>
              我们按三类受众重新做了内容策略。一篇帖子想覆盖所有人，那是不可能的。给采购工程师看的内容讲认证流程、交付周期。研发总监那边看材料性能指标，耐热数据、应力试验结果这些。供应链负责人关心的是生产一致性和交货稳不稳定。
            </p>
            <p>
              内容的原则就一条：<strong>技术上要站得住，表达上别让人看不下去。</strong>缩写不能乱堆，用了就得解释清楚。有一篇帖子拆解了机翼组件里两类聚合物家族到底有什么区别，写出来像一篇三分钟就能读完的科普文，不是参数表。那篇帖子成了前两个月自然流量的冠军，我们自己也没想到，本来以为一篇讲认证流程的会更好。
            </p>
            <p>
              推广广告在四个市场同时投。Lead Gen表单提供一份材料性能对比工具，是工程师拿到手真的会存下来用的参考资料，不是什么宣传册。
            </p>
          </section>

          <section>
            <h2>收获</h2>
            <p>
              <strong>有一家世界500强的航空航天企业直接通过LinkedIn找上来了。</strong>这种事以前从来没发生过。客户每次聊起这个项目，头一件事就是讲这个。
            </p>
            <p>
              具体数据的话：页面从340涨到1,900粉丝，自然帖子平均互动率3.2%。广告覆盖了14,000名航空航天行业的人，带来61条线索，单条58美元。获客单价比我们一开始估的高了一些，我们估的是四十多美元，但线索质量过硬，客户这边没什么异议。
            </p>
            <blockquote>
              "现在采购方开会的时候会主动提到我们LinkedIn上的帖子。以前不可能的。"
            </blockquote>
          </section>

        </div>
      </div>
    </article>

    <!-- Back link -->
    <section class="cs-back" aria-label="导航">
      <div class="container">
        <a href="/zh/work" class="cs-back__link">← 返回全部案例</a>
      </div>
    </section>

    <ContactCTA lang="zh" />
  </main>
</Layout>

<style>
  /* ── Hero ── */
  .cs-hero {
    background: var(--deep-navy);
    padding-block: clamp(3rem, 6vw, 5rem) clamp(3rem, 5vw, 4.5rem);
    position: relative;
    overflow: hidden;
  }
  .cs-hero::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse 60% 80% at 80% 30%, rgba(6,182,212,0.10) 0%, transparent 60%);
    pointer-events: none;
  }

  .cs-hero__inner {
    position: relative;
    z-index: 1;
    max-width: 780px;
  }

  .cs-hero__meta {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--sp-4);
    margin-bottom: var(--sp-6);
  }

  .cs-hero__number {
    font-family: var(--font-body);
    font-size: var(--fs-xs);
    font-weight: 700;
    letter-spacing: 0.10em;
    text-transform: uppercase;
    color: var(--cyan);
  }

  .cs-hero__tags {
    display: flex;
    flex-wrap: wrap;
    gap: var(--sp-2);
  }

  .cs-tag-pill {
    display: inline-block;
    padding: 2px 10px;
    border-radius: 999px;
    background: rgba(255,255,255,0.10);
    border: 1px solid rgba(255,255,255,0.16);
    color: rgba(255,255,255,0.75);
    font-family: var(--font-body);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .cs-hero__h1 {
    font-family: var(--font-display);
    font-size: clamp(2rem, 4.5vw, 3.25rem);
    font-weight: 700;
    line-height: 1.12;
    letter-spacing: -0.01em;
    color: var(--white);
    margin-bottom: var(--sp-6);
  }
  .cs-hero__h1 em {
    color: rgba(255,255,255,0.45);
    font-weight: 300;
    font-style: normal;
  }

  .cs-hero__deck {
    font-family: var(--font-body);
    font-size: var(--fs-md);
    color: rgba(255,255,255,0.68);
    line-height: 1.75;
    max-width: 600px;
  }

  /* ── Stats bar ── */
  .cs-stats-bar {
    background: var(--off-white);
    border-bottom: 1px solid var(--border);
    padding-block: var(--sp-8);
  }

  .cs-stats-bar__inner {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--sp-6);
    max-width: 680px;
    margin-inline: auto;
  }

  .cs-stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 4px;
  }

  .cs-stat__value {
    font-family: var(--font-display);
    font-size: clamp(1.75rem, 3vw, 2.25rem);
    font-weight: 700;
    color: var(--deep-navy);
    letter-spacing: -0.02em;
  }

  .cs-stat__label {
    font-family: var(--font-body);
    font-size: 0.72rem;
    color: var(--text-3);
    font-weight: 500;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  /* ── Body ── */
  .cs-body {
    padding-block: clamp(3rem, 6vw, 5rem);
    background: var(--white);
  }

  .cs-content {
    max-width: 680px;
    margin-inline: auto;
  }

  .cs-content section {
    margin-bottom: clamp(2.5rem, 4vw, 3.5rem);
  }

  .cs-content h2 {
    font-family: var(--font-display);
    font-size: clamp(1.25rem, 2vw, 1.6rem);
    font-weight: 700;
    color: var(--deep-navy);
    letter-spacing: -0.01em;
    line-height: 1.3;
    margin-bottom: var(--sp-5);
  }

  .cs-content p {
    font-family: var(--font-body);
    font-size: var(--fs-md);
    color: var(--text);
    line-height: 1.85;
    margin-bottom: var(--sp-5);
  }

  .cs-content blockquote {
    border-left: 3px solid var(--sky);
    margin: var(--sp-8) 0;
    padding: var(--sp-1) var(--sp-6);
    font-family: var(--font-body);
    font-size: var(--fs-md);
    font-style: italic;
    color: var(--text-2);
    line-height: 1.75;
  }

  /* ── Back link ── */
  .cs-back {
    padding-block: var(--sp-8);
    background: var(--off-white);
    border-top: 1px solid var(--border);
  }

  .cs-back__link {
    font-family: var(--font-body);
    font-size: var(--fs-sm);
    font-weight: 500;
    color: var(--text-2);
    transition: color var(--t-fast), transform var(--t-fast);
  }
  .cs-back__link:hover { color: var(--sky); transform: translateY(-3px); }

  /* ── Responsive ── */
  @media (max-width: 640px) {
    .cs-hero__meta { flex-direction: column; align-items: flex-start; }
    .cs-stats-bar__inner { grid-template-columns: 1fr; gap: var(--sp-5); }
  }
</style>
