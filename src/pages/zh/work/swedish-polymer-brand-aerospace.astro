---
import Layout from '../../../layouts/Layout.astro';
import NavBar from '../../../components/NavBar.astro';
import ContactCTA from '../../../components/ContactCTA.astro';
---

<Layout
  lang="zh"
  title="瑞典聚合物品牌航空航天获客实录 | Nuvora Studio"
  description="一家瑞典高分子材料厂商成了航空采购商在LinkedIn上最先搜到的品牌。从340个粉丝到世界500强主动上门。"
  abstract="实战复盘：咨询+LinkedIn内容+广告如何帮瑞典航空航天材料品牌建立行业话语权。"
>
  <NavBar lang="zh" />
  <main id="main-content">

    <!-- Hero -->
    <section class="cs-hero" aria-labelledby="cs-heading">
      <div class="container">
        <div class="cs-hero__inner">
          <div class="cs-hero__meta">
            <span class="cs-hero__number">实战案例</span>
            <div class="cs-hero__tags">
              <span class="cs-tag-pill">咨询</span>
              <span class="cs-tag-pill">LinkedIn内容运营</span>
              <span class="cs-tag-pill">LinkedIn广告投放</span>
            </div>
          </div>
          <h1 id="cs-heading" class="cs-hero__h1">
            瑞典聚合物厂商，怎么让航空采购第一个想到自己？<br /><em>主页340个粉丝的时候，没人信。</em>
          </h1>
          <p class="cs-hero__deck">
            产品装在飞机上飞，但LinkedIn主页像个空壳。
          </p>
        </div>
      </div>
    </section>

    <!-- Stats bar -->
    <section class="cs-stats-bar" aria-label="核心指标">
      <div class="container">
        <div class="cs-stats-bar__inner">
          <div class="cs-stat">
            <span class="cs-stat__value">1,900</span>
            <span class="cs-stat__label">新增粉丝</span>
          </div>
          <div class="cs-stat">
            <span class="cs-stat__value">3.2%</span>
            <span class="cs-stat__label">互动率</span>
          </div>
          <div class="cs-stat">
            <span class="cs-stat__value">61</span>
            <span class="cs-stat__label">销售线索</span>
          </div>
        </div>
      </div>
    </section>

    <!-- Article body -->
    <article class="cs-body" aria-label="案例正文">
      <div class="container">
        <div class="cs-content">

          <section>
            <h2>背景</h2>
            <p>
              瑞典的一家高性能聚合物复合材料制造商，给飞机做零部件材料。他们的材料此刻就在天上飞着。但LinkedIn主页完全看不出这些——340个粉丝，没有发帖节奏，品牌视觉好几年没换过。另一边，一个产品线比它还窄的竞争对手，粉丝是它三倍，每个季度都在投LinkedIn广告。
            </p>
            <blockquote>
              "有些单子我们本来该拿下的，结果丢了。因为采购方去LinkedIn一看，觉得我们像个小作坊。"
            </blockquote>
          </section>

          <section>
            <h2>怎么做的</h2>
            <p>
              从咨询项目切入。做了完整的LinkedIn诊断、竞品拆解、90天行动计划。诊断结果证实了销售总监自己早就知道的事：<strong>页面上发的东西全是自说自话。</strong>公司获了什么奖、办公室翻新了、成立周年庆。没有哪条是采购方会关心的。面向采购工程师的内容？没有。面向研发总监的材料评估参考？也没有。
            </p>
            <p>
              我们给三类受众分别重新设计了内容策略，不再妄想一篇帖子覆盖所有人。给采购工程师看的内容讲认证流程和交付周期。给研发总监看的是材料性能指标——耐热数据、应力试验结果。给供应链负责人看的是生产一致性和交货稳定性。
            </p>
            <p>
              内容的原则就一条：<strong>技术上让人服，文字上让人看得进去。</strong>不堆缩写，用了就解释。有一篇帖子拆解了机翼组件里两类聚合物家族的本质区别，写出来像一篇三分钟能读完的科普，不是一份参数表。那篇帖子成了前两个月自然流量表现最好的一条——说实话有点意外，我们原本押注的是一篇讲认证的帖子。
            </p>
            <p>
              推广广告在四个市场同时投。Lead Gen表单提供的是一份材料性能对比工具——工程师拿到手真的会存下来用的东西，不是一本宣传册。
            </p>
          </section>

          <section>
            <h2>结果</h2>
            <p>
              <strong>一家世界500强航空航天企业通过LinkedIn主动联系了过来。</strong>以前从来没有发生过这种事。客户每次跟人提起这个项目，第一个说的就是这件事。
            </p>
            <p>
              数据：页面从340涨到1,900粉丝。自然帖子平均互动率3.2%。广告覆盖了14,000名航空航天从业者，带来61条线索，每条58美元。获客单价比我们最初预估的高了一截（我们估的是四十多美元），但线索质量过硬，客户没意见。
            </p>
            <blockquote>
              "现在采购方开会的时候会主动提到我们LinkedIn上的帖子。以前不可能的。"
            </blockquote>
          </section>

        </div>
      </div>
    </article>

    <!-- Back link -->
    <section class="cs-back" aria-label="导航">
      <div class="container">
        <a href="/zh/work" class="cs-back__link">← 返回全部案例</a>
      </div>
    </section>

    <ContactCTA lang="zh" />
  </main>
</Layout>

<style>
  /* ── Hero ── */
  .cs-hero {
    background: var(--deep-navy);
    padding-block: clamp(3rem, 6vw, 5rem) clamp(3rem, 5vw, 4.5rem);
    position: relative;
    overflow: hidden;
  }
  .cs-hero::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse 60% 80% at 80% 30%, rgba(6,182,212,0.10) 0%, transparent 60%);
    pointer-events: none;
  }

  .cs-hero__inner {
    position: relative;
    z-index: 1;
    max-width: 780px;
  }

  .cs-hero__meta {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--sp-4);
    margin-bottom: var(--sp-6);
  }

  .cs-hero__number {
    font-family: var(--font-body);
    font-size: var(--fs-xs);
    font-weight: 700;
    letter-spacing: 0.10em;
    text-transform: uppercase;
    color: var(--cyan);
  }

  .cs-hero__tags {
    display: flex;
    flex-wrap: wrap;
    gap: var(--sp-2);
  }

  .cs-tag-pill {
    display: inline-block;
    padding: 2px 10px;
    border-radius: 999px;
    background: rgba(255,255,255,0.10);
    border: 1px solid rgba(255,255,255,0.16);
    color: rgba(255,255,255,0.75);
    font-family: var(--font-body);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .cs-hero__h1 {
    font-family: var(--font-display);
    font-size: clamp(2rem, 4.5vw, 3.25rem);
    font-weight: 700;
    line-height: 1.12;
    letter-spacing: -0.01em;
    color: var(--white);
    margin-bottom: var(--sp-6);
  }
  .cs-hero__h1 em {
    color: rgba(255,255,255,0.45);
    font-weight: 300;
    font-style: normal;
  }

  .cs-hero__deck {
    font-family: var(--font-body);
    font-size: var(--fs-md);
    color: rgba(255,255,255,0.68);
    line-height: 1.75;
    max-width: 600px;
  }

  /* ── Stats bar ── */
  .cs-stats-bar {
    background: var(--off-white);
    border-bottom: 1px solid var(--border);
    padding-block: var(--sp-8);
  }

  .cs-stats-bar__inner {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--sp-6);
    max-width: 680px;
    margin-inline: auto;
  }

  .cs-stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 4px;
  }

  .cs-stat__value {
    font-family: var(--font-display);
    font-size: clamp(1.75rem, 3vw, 2.25rem);
    font-weight: 700;
    color: var(--deep-navy);
    letter-spacing: -0.02em;
  }

  .cs-stat__label {
    font-family: var(--font-body);
    font-size: 0.72rem;
    color: var(--text-3);
    font-weight: 500;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  /* ── Body ── */
  .cs-body {
    padding-block: clamp(3rem, 6vw, 5rem);
    background: var(--white);
  }

  .cs-content {
    max-width: 680px;
    margin-inline: auto;
  }

  .cs-content section {
    margin-bottom: clamp(2.5rem, 4vw, 3.5rem);
  }

  .cs-content h2 {
    font-family: var(--font-display);
    font-size: clamp(1.25rem, 2vw, 1.6rem);
    font-weight: 700;
    color: var(--deep-navy);
    letter-spacing: -0.01em;
    line-height: 1.3;
    margin-bottom: var(--sp-5);
  }

  .cs-content p {
    font-family: var(--font-body);
    font-size: var(--fs-md);
    color: var(--text);
    line-height: 1.85;
    margin-bottom: var(--sp-5);
  }

  .cs-content blockquote {
    border-left: 3px solid var(--sky);
    margin: var(--sp-8) 0;
    padding: var(--sp-1) var(--sp-6);
    font-family: var(--font-body);
    font-size: var(--fs-md);
    font-style: italic;
    color: var(--text-2);
    line-height: 1.75;
  }

  /* ── Back link ── */
  .cs-back {
    padding-block: var(--sp-8);
    background: var(--off-white);
    border-top: 1px solid var(--border);
  }

  .cs-back__link {
    font-family: var(--font-body);
    font-size: var(--fs-sm);
    font-weight: 500;
    color: var(--text-2);
    transition: color var(--t-fast), transform var(--t-fast);
  }
  .cs-back__link:hover { color: var(--sky); transform: translateY(-3px); }

  /* ── Responsive ── */
  @media (max-width: 640px) {
    .cs-hero__meta { flex-direction: column; align-items: flex-start; }
    .cs-stats-bar__inner { grid-template-columns: 1fr; gap: var(--sp-5); }
  }
</style>
