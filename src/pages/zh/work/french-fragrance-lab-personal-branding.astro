---
import Layout from '../../../layouts/Layout.astro';
import NavBar from '../../../components/NavBar.astro';
import ContactCTA from '../../../components/ContactCTA.astro';
---

<Layout
  lang="zh"
  title="法国调香师靠LinkedIn个人品牌拿到奢侈品客户 | Nuvora Studio"
  description="一家法国调香实验室，凭创始人在LinkedIn上的个人表达，吸引到了奢侈品牌客户。群发邮件没人理，个人IP打开了每一扇门。"
  abstract="一个调香师怎么通过LinkedIn个人品牌，让奢侈品牌主动找上门。"
>
  <NavBar lang="zh" />
  <main id="main-content">

    <!-- Hero -->
    <section class="cs-hero" aria-labelledby="cs-heading">
      <div class="container">
        <div class="cs-hero__inner">
          <div class="cs-hero__meta">
            <span class="cs-hero__number">客户案例</span>
            <div class="cs-hero__tags">
              <span class="cs-tag-pill">高管IP打造</span>
              <span class="cs-tag-pill">LinkedInLinkedIn内容服务</span>
              <span class="cs-tag-pill">咨询</span>
            </div>
          </div>
          <h1 id="cs-heading" class="cs-hero__h1">
            法国调香师在LinkedIn上开了口，奢侈品客户自己找上来了。<br /><em>群发邮件没人理，一个人的声音解决了问题。</em>
          </h1>
          <p class="cs-hero__deck">
            冷邮件无效，通过创始人个人品牌打开局面。
          </p>
        </div>
      </div>
    </section>

    <!-- Stats bar -->
    <section class="cs-stats-bar" aria-label="核心指标">
      <div class="container">
        <div class="cs-stats-bar__inner">
          <div class="cs-stat">
            <span class="cs-stat__value">2,400</span>
            <span class="cs-stat__label">4 个月增长粉丝</span>
          </div>
          <div class="cs-stat">
            <span class="cs-stat__value">8,000+</span>
            <span class="cs-stat__label">单条内容最高曝光</span>
          </div>
          <div class="cs-stat">
            <span class="cs-stat__value">2</span>
            <span class="cs-stat__label">个主动咨询机会</span>
          </div>
        </div>
      </div>
    </section>

    <!-- Article body -->
    <article class="cs-body" aria-label="案例正文">
      <div class="container">
        <div class="cs-content">

          <section>
            <h2>背景</h2>
            <p>
              法国一家B2B调香实验室，给奢侈品牌、高端酒店、化妆品集团做定制香型。创始人是科班出身的调香师，入行二十多年，圈内有名气。但圈子外头，谁都不认识她。新客户主要靠熟人介绍，再加上一年一次的行业大展。展会一过，管线就安静了。<strong>发给奢侈品牌创意总监的陌生开发邮件，回复率一直是零。</strong>做的活儿值得被更多人看到。
            </p>
          </section>

          <section>
            <h2>怎么切入的</h2>
            <p>
              一开始就按高管IP项目来做。创始人专业积淀够深，而且她自己也想做这件事（这点很关键）。不需要花力气说服。她自己就挺郁闷的，觉得自己知道的东西只能传递到身边本来就认识的那些人。
            </p>
            <p>
              头两周都花在定位上了。"调香实验室创始人"太泛了，说了跟没说差不多。最后找到一个更准的角度：<strong>为奢侈品牌创造气味这件事，背后是科学和直觉在来回拉扯。</strong>有了这个框架，她就可以写自己每天真正在想的事了。
            </p>
            <p>
              她写的东西很杂但都很真实。产地怎么影响原料品质，一个客户需求怎么经过四五轮调整变成成品香水，为什么有些香型趋势能从时装传到酒店行业、另一些却很快消失。她写去卡拉布里亚的佛手柑庄园做溯源的事，也写过客户简报上赫然写着"希望闻起来像信任"要怎么办（这种事比你以为的常见得多）。
            </p>
            <p>
              这些帖子不是按照什么内容日历批量排出来的，都是从她的日常工作里来的。我们做的就是帮她润色一下、把文字收紧、安排好发布节奏。但声音是她自己的，这一点从头到尾没变过。
            </p>
            <p>
              企业页面同步在跑，发研发流程、原料采购、项目手记一类的内容。
            </p>
            <p>
              咨询项目把整个策略锚定在商业目标上。理想客户画像、信息传达框架、一份团队能自己跑的6个月路线图。
            </p>
          </section>

          <section>
            <h2>结果怎么样</h2>
            <p>
              4个月涨了2,400粉丝。有两篇帖子阅读量过了8,000。企业页面那边新增480粉。增速确实比我们手上其他项目慢，但这个受众面实在太窄了，奢侈品调香行业本来也不是什么大众赛道。
            </p>
            <p>
              对她来说最重要的事：<strong>一家国际奢侈化妆品集团和一家跨国酒店管理公司在LinkedIn上主动发来了私信</strong>，两边都要提交开发方案。没有冷邮件，没有展会。
            </p>
            <blockquote>
              "LinkedIn就是一场永远不散的行业展会。"
            </blockquote>
          </section>

        </div>
      </div>
    </article>

    <!-- Back link -->
    <section class="cs-back" aria-label="导航">
      <div class="container">
        <a href="/zh/work" class="cs-back__link">← 返回全部案例</a>
      </div>
    </section>

    <ContactCTA lang="zh" />
  </main>
</Layout>

<style>
  /* ── Hero ── */
  .cs-hero {
    background: var(--deep-navy);
    padding-block: clamp(3rem, 6vw, 5rem) clamp(3rem, 5vw, 4.5rem);
    position: relative;
    overflow: hidden;
  }
  .cs-hero::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse 60% 80% at 80% 30%, rgba(6,182,212,0.10) 0%, transparent 60%);
    pointer-events: none;
  }

  .cs-hero__inner {
    position: relative;
    z-index: 1;
    max-width: 780px;
  }

  .cs-hero__meta {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--sp-4);
    margin-bottom: var(--sp-6);
  }

  .cs-hero__number {
    font-family: var(--font-body);
    font-size: var(--fs-xs);
    font-weight: 700;
    letter-spacing: 0.10em;
    text-transform: uppercase;
    color: var(--cyan);
  }

  .cs-hero__tags {
    display: flex;
    flex-wrap: wrap;
    gap: var(--sp-2);
  }

  .cs-tag-pill {
    display: inline-block;
    padding: 2px 10px;
    border-radius: 999px;
    background: rgba(255,255,255,0.10);
    border: 1px solid rgba(255,255,255,0.16);
    color: rgba(255,255,255,0.75);
    font-family: var(--font-body);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .cs-hero__h1 {
    font-family: var(--font-display);
    font-size: clamp(2rem, 4.5vw, 3.25rem);
    font-weight: 700;
    line-height: 1.12;
    letter-spacing: -0.01em;
    color: var(--white);
    margin-bottom: var(--sp-6);
  }
  .cs-hero__h1 em {
    color: rgba(255,255,255,0.45);
    font-weight: 300;
    font-style: normal;
  }

  .cs-hero__deck {
    font-family: var(--font-body);
    font-size: var(--fs-md);
    color: rgba(255,255,255,0.68);
    line-height: 1.75;
    max-width: 600px;
  }

  /* ── Stats bar ── */
  .cs-stats-bar {
    background: var(--off-white);
    border-bottom: 1px solid var(--border);
    padding-block: var(--sp-8);
  }

  .cs-stats-bar__inner {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--sp-6);
    max-width: 680px;
    margin-inline: auto;
  }

  .cs-stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 4px;
  }

  .cs-stat__value {
    font-family: var(--font-display);
    font-size: clamp(1.75rem, 3vw, 2.25rem);
    font-weight: 700;
    color: var(--deep-navy);
    letter-spacing: -0.02em;
  }

  .cs-stat__label {
    font-family: var(--font-body);
    font-size: 0.72rem;
    color: var(--text-3);
    font-weight: 500;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  /* ── Body ── */
  .cs-body {
    padding-block: clamp(3rem, 6vw, 5rem);
    background: var(--white);
  }

  .cs-content {
    max-width: 680px;
    margin-inline: auto;
  }

  .cs-content section {
    margin-bottom: clamp(2.5rem, 4vw, 3.5rem);
  }

  .cs-content h2 {
    font-family: var(--font-display);
    font-size: clamp(1.25rem, 2vw, 1.6rem);
    font-weight: 700;
    color: var(--deep-navy);
    letter-spacing: -0.01em;
    line-height: 1.3;
    margin-bottom: var(--sp-5);
  }

  .cs-content p {
    font-family: var(--font-body);
    font-size: var(--fs-md);
    color: var(--text);
    line-height: 1.85;
    margin-bottom: var(--sp-5);
  }

  .cs-content blockquote {
    border-left: 3px solid var(--sky);
    margin: var(--sp-8) 0;
    padding: var(--sp-1) var(--sp-6);
    font-family: var(--font-body);
    font-size: var(--fs-md);
    font-style: italic;
    color: var(--text-2);
    line-height: 1.75;
  }

  /* ── Back link ── */
  .cs-back {
    padding-block: var(--sp-8);
    background: var(--off-white);
    border-top: 1px solid var(--border);
  }

  .cs-back__link {
    font-family: var(--font-body);
    font-size: var(--fs-sm);
    font-weight: 500;
    color: var(--text-2);
    transition: color var(--t-fast), transform var(--t-fast);
  }
  .cs-back__link:hover { color: var(--sky); transform: translateY(-3px); }

  /* ── Responsive ── */
  @media (max-width: 640px) {
    .cs-hero__meta { flex-direction: column; align-items: flex-start; }
    .cs-stats-bar__inner { grid-template-columns: 1fr; gap: var(--sp-5); }
  }
</style>
